<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!--
      Launch theme. Shows the MeshVoice emblem via the AndroidX SplashScreen
      API from the instant the icon is tapped, then hands off to
      Theme.MeshVoice once MainActivity is ready.
    -->
    <style name="Theme.MeshVoice.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_icon</item>
        <item name="windowSplashScreenAnimationDuration">600</item>
        <item name="postSplashScreenTheme">@style/Theme.MeshVoice</item>
    </style>

    <style name="Theme.MeshVoice" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="colorPrimary">@color/accent</item>
        <item name="colorPrimaryVariant">@color/accent</item>
        <item name="colorOnPrimary">@color/black</item>
        <item name="android:windowBackground">@color/surface</item>
        <item name="android:navigationBarColor">@color/surface</item>
        <item name="android:statusBarColor">@color/surface</item>
        <item name="android:fontFamily">sans</item>
    </style>

</resources>
